<Window x:Class="RomValidator.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About ROM Validator"
        Width="600"
        Height="500"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10,20,10,10" HorizontalAlignment="Center" Orientation="Horizontal">
            <Image Width="128" Height="128" Source="pack://application:,,,/images/logo.png" ToolTip="ROM Validator Logo" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="20,10,20,10">
            <TextBlock Style="{StaticResource SubHeaderTextBlockStyle}" Text="ROM Validator" />
            <TextBlock x:Name="DescriptionTextBlock" TextWrapping="Wrap" Margin="0,5,0,0" 
                       Text="A utility for validating ROM files against DAT files and generating new No-Intro compliant DAT files from your collection." />
            <TextBlock Name="AppVersionTextBlock" Margin="0,10,0,0" />
            <TextBlock Margin="0,10,0,0" Text="This tool helps ensure your ROM collection is complete, accurate, and properly cataloged." />

            <TextBlock Margin="0,10,0,0">
                <Hyperlink NavigateUri="http://www.purelogiccode.com" RequestNavigate="Hyperlink_RequestNavigate" ToolTip="Visit the official website">
                    <TextBlock Text="http://www.purelogiccode.com" />
                </Hyperlink>
            </TextBlock>

            <TextBlock Margin="0,5,0,0">
                <Hyperlink NavigateUri="https://github.com/drpetersonfernandes/RomValidator" RequestNavigate="Hyperlink_RequestNavigate" ToolTip="Visit the GitHub repository (source code, releases, issues)">
                    <TextBlock Text="https://github.com/drpetersonfernandes/RomValidator" />
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <Button Grid.Row="2" Margin="10,10,10,20" Click="CloseButton_Click" Content="Close" ToolTip="Close this window" Style="{StaticResource CloseButtonStyle}" />
    </Grid>
</Window>
